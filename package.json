{
  "name": "evolution-engine",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "turbo run dev",
    "dev:studio": "turbo run dev --filter=studio",
    "dev:owners": "turbo run dev --filter=owners",
    "dev:valuation": "turbo run dev --filter=valuation",
    "build": "turbo run build",
    "build:studio": "turbo run build --filter=studio",
    "build:owners": "turbo run build --filter=owners",
    "build:valuation": "turbo run build --filter=valuation",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Evolution Engine - Modular platform for Studio, Owners, and Valuation"
}
